#ifndef GTKMMPROJECT_INSTANCE_H
#define GTKMMPROJECT_INSTANCE_H
    class bitenger_instance{
    public:
        bitenger_instance(){
            // Do all of the crap in here.
            // On first run, get the ip address and broadcast the address to all substations in the
            // local network.

            // Find current alive peers and add them to the peer list.


            // Update the local copy of the blockchain in substation.

            // Get the current index of the message and verify it with nearest substation for recent copy.
            // i.e. Do a network sweep for the thing.
        }
    };
#endif